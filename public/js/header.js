// header.js

document.addEventListener('DOMContentLoaded', function() {
    const userElement = document.getElementById('username');
    if (userElement) {
        userElement.innerText = user.username; // Cambiar 'user.username' por el campo que contiene el nombre de usuario en tu aplicación
    }
});
