<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToolBox - Editar producto</title>

    <link rel="icon" type="image/x-icon" href="/img/logo/Logo 180.png">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="/css/cuenta.css">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <script type="module" src="/js/editar.js"></script> <!-- O la ruta donde tengas tus scripts -->

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Arimo:ital,wght@0,400..700&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <nav class="navbar">
            <div class="container-fluid">
              <a class="navbar-brand text-light" href="#" id="admin">
                <img src="/img/logo/Logo trazo 180.png" alt="Logo" height="45" class="w-25 d-inline-block align-text-top">
                ToolBox Account
              </a>
            </div>
        </nav>
    </header>

    <div>
        <main class="px-5 py-2">
            <h2>Editar producto</h2>

            <div class="flex flex-col mt-10 items-center">
                <div class="-my-2 py-2 overflow-x-auto sm:-mx-6 sm:px-6 lg:-mx-8 lg:px-8 w-10/12 md:w-8/12 lg:w-6/12">
                    <div class="shadow overflow-hidden sm:rounded-lg border-b border-gray-200">
                        
                        <form id="formulario" data-id="<%= producto._id %>" method="POST" action="#">
                            <!-- Campos para editar -->
                            <input id="nombre" name="nombre" type="text" value="<%= producto.nombre %>" />
                            <input id="precio" name="precio" type="number" value="<%= producto.precio %>" />
                            <input id="categoria" name="categoria" type="text" value="<%= producto.categoria %>" />
                            <textarea id="desc" name="descripcion"><%= producto.descripcion %></textarea>
                            <!-- Botones de guardar y cancelar -->
                            <button type="submit">Guardar Cambios</button>
                            <button type="button" id="cancelar">Cancelar Edición</button>
                        </form>
                        
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Bootstrap JS (si es necesario) -->
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> -->
    
    <!-- Scripts personalizados -->
    <!-- <script src="/js/editar.js"></script> -->
    
</body>
</html>